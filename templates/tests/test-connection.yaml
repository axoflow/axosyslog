apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "syslog-ng-chart.fullname" . }}-test-connection"
  labels:
    {{- include "syslog-ng-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers: {}
    # ctl healtcheck
    # - name: wget
    #   image: busybox
    #   command: ['wget']
    #   args: ['{{ include "syslog-ng-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
