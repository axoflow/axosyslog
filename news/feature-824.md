`http()`, `otel()` and other threaded destinations: add prometheus style
histograms in the `syslog-ng-ctl stats prometheus` output for the following
metrics: `batch_size`, `message_size`, `request_latency`.
